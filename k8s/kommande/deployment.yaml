apiVersion: apps/v1
kind: Deployment
metadata:
  name: kommande
  namespace: kommande
  labels:
    app: kommande
spec:
  revisionHistoryLimit: 0
  replicas: 1
  selector:
    matchLabels:
      app: kommande
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: kommande
    spec:
      imagePullSecrets:
        - name: ghcr
      containers:
        - name: kommande
          image: ghcr.io/therayquaza/home-cluster/kommande:v0.3.2@sha256:b143622ab1784c11a4f68bf3c37017c6e673b17299a4d70f3e53fa069a2f7117
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            seccompProfile:
              type: RuntimeDefault
          envFrom:
            - configMapRef:
                name: kommande-config
            - secretRef:
                name: kommande-oidc-secret
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 30

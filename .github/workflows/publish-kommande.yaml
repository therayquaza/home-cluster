name: Publish - kommande

on:
  push:
    tags:
      - "kommande/v*"

jobs:
  build:
    uses: ./.github/workflows/reusable-build.yaml
    with:
      context: apps/kommande
      image-name: ghcr.io/therayquaza/home-cluster/kommande
      push: true
      tag-prefix: "kommande/"
    permissions:
      contents: read
      packages: write

  attest:
    needs: build
    runs-on: [home-cluster]
    permissions:
      id-token: write
      attestations: write
      packages: write
    steps:
      - name: Attest build provenance
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ghcr.io/therayquaza/home-cluster/kommande
          subject-digest: ${{ needs.build.outputs.digest }}
          push-to-registry: true
